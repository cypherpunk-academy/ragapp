# config/

App-Build-Konfiguration (falls später benötigt).

**Design-Tokens** liegen unter [`../design/tokens/`](../design/tokens/).

`material-theme.json` in diesem Ordner ist ein Symlink auf `design/tokens/material-theme.json`.
